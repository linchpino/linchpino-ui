export default function PrivatePage({
                                        children,
                                    }: Readonly<{ children: React.ReactNode }>) {
    return (
        {children}
    )
}
